#!/bin/sh
case "$(uname -sm)" in
  "Darwin arm64") exec "$0-darwin-arm64" "$@" ;;
  "Linux aarch64") exec "$0-linux-arm64" "$@" ;;
  "Linux x86_64") exec "$0-linux-amd64" "$@" ;;
  *) echo "unsupported: $(uname -sm)" >&2; exit 1 ;;
esac
