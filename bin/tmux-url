#!/bin/sh

# Capture visible pane content and extract URLs
tmux capture-pane -p | \
    grep -oE 'https?://[^ ]+' | \
    sed 's/[>).,;:]+$//' | \
    sort -u | \
    fzf --height=50% --reverse | \
    xargs -r open
